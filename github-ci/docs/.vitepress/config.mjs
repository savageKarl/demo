import { defineConfig } from "vitepress";

// https://vitepress.dev/reference/site-config
export default defineConfig({
	title: "demo-github-ci",
	description: "this just a demo",
	base: "/demo-github-ci/",
});
